// Склады которые у нас есть
const Warehouse = {
    1 : {
     name : string,
     id_warehouse : number,
     garage : {
        // presence - Есть ли машина уже на складе
        // Если нет то time говорит когда машина прибудет
        1 : { id_car : number, presence : Bool, time : number}
     },
     products : {
      1 : { id_product : string, count : number},
     } 
    },
}

// Машины которые есть у компании
const Cars = {
    1 : { id_car : number, max_weight : number}
} 

// Продукты которые хранятся на складах
const Products = {
 1 : {id_product : string, name: string, weight : number}
}

// Расстояния между скадами
Distances = {
 // time - в минутах
 1 : {
  from : id_warehouse,
  to : id_werehouse,
  time : number
 }
}

